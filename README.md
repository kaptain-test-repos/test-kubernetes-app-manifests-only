# Test Kubernetes App Manifests Only

E2E test repo for `kubernetes-app-manifests-only` workflow in buildon-github-actions.

Tests allow-builtin-token-override, github-actions token style, cronjob workload type with schedule/suspend tokens, and the template project pattern (token override flow defers docker image tokens to downstream).
